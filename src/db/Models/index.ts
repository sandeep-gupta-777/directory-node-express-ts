import {dirEntryModel,IDirEntry} from './directory'
import {dirUserModel,IDirUser} from './directory-user'

type TModel  =  IDirEntry | IDirUser;

export {
    dirEntryModel,
    IDirEntry,
    dirUserModel,
    IDirUser,
    TModel
}
